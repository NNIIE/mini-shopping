bootJar.enabled = false         // Spring Boot Jar 빌드 비활성화 - 독립적 실행 X
jar.enabled = true              // Jar 빌드 활성화 - 다른 모듈에서 사용가능

dependencies {
    api("org.springframework.boot:spring-boot-starter-data-jpa")    // api() 의존성 선언 (의존하는 다른 모듈에도 전이)
    runtimeOnly("com.mysql:mysql-connector-j")
}
